package com.ms.dob.photocollage.photopicker;

import android.graphics.Color;


public interface Define {
  String PATHS = "paths",
      PHOTOS = "photos",
      CONFIGURE = "configure",
      DEFAULT_ALBUM_TITLE = "Please select an album",
      DEFAULT_PHOTO_TITLE = "Please select a photo",
      DEFAULT_MAX_NOTICE = "Please Select only one image";

  int DEFAULT_REQUEST_CODE = 94;

  int DEFAULT_REQUEST_PERMISSION_CODE = 77;

  int DEFAULT_TOOLBAR_COLOR = Color.BLACK;

  int DEFAULT_TITLE_COLOR = Color.WHITE;

  int DEFAULT_STATUS_BAR_COLOR = Color.BLACK;

  int DEFAULT_MAX_COUNT = 10;

  int DEFAULT_DIVIDER_LINE_SIZE = 1;

  int DEFAULT_DIVIDER_LINE_COLOR = Color.BLACK;
}
